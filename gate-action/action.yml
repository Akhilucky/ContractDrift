name: "Contract Sentinel Gate"
description: "Check contract drift before promoting"
inputs:
  service:
    description: "Service ID"
    required: true
  version:
    description: "Version SHA"
    required: true
  environment:
    description: "Target environment"
    required: true
  gate_url:
    description: "Promotion Gate URL"
    required: true
runs:
  using: "docker"
  image: "Dockerfile"
